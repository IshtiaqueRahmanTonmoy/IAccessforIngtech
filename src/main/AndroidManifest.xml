<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="iaccess.iaccess.com.iaccess">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".DashboardActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_dashboard"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AttendanceActivity" />
        <activity android:name=".AttendanceHistoryActivity" />
        <activity android:name=".AttendanceDetails" />
        <activity android:name=".MapActivity" />
        <activity android:name=".AddSupportActivity" />
        <activity android:name=".SupportHistory" />
        <activity android:name=".SupportDetailsActivity"></activity>
    </application>

</manifest>